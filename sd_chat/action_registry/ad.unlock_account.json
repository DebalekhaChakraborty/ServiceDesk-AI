{
  "id": "ad.unlock_account",
  "title": "Unlock the target Active Directory account",
  "description": "Checks the current lock state, unlocks the target Active Directory Domain Services user account only if necessary, and verifies the final lock state without enabling a disabled account or resetting its password.",
  "inputs": ["target_upn"],
  "outputs": ["unlock.status", "unlock.message"],
  "preconditions": ["caller_is_self_or_manager"],
  "effects": ["account_unlocked(${target_upn})"],
  "tool": "ad_account_tool",
  "action": "unlock_account",
  "args_template": {
    "target_upn": "${target_upn}"
  }
}
