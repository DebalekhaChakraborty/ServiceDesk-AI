node_modules/
coverage/
npm-debug.log*

# Real environment files never belong in Git. The placeholder template does.
.env
.env.local
.env.*.local
!.env.example

# Local-only runtime state for the on-VM portal (Phase 5B). Never committed.
runtime/
